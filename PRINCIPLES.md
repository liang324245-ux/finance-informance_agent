# 财经新闻 Agent — 项目底层设计原则

> 本文件是整个项目的"宪法"。后续任何代码、方案、路径选择，都必须先通过这三条原则的检验。
> 代码只是系统的一部分，不是全部——系统还包括：信息源策略、提示词、配置、容错机制、文档与可维护性。

---

## 核心原则

### 1. 稳定性（Stability）
系统要能长期可靠地自动运行，而不是"今天能跑、明天就挂"。

具体要求：
- **不把命押在单一来源上。** 任何外部依赖（新闻源、API）都要有备份或降级方案，单点失效不能让整个系统瘫痪。
- **容错优先。** 外部世界一定会出问题（源失效、网络超时、接口报错），代码必须预判这些情况并优雅处理，而不是直接崩溃。
- **失败要可见。** 出问题时要有清晰的日志/提示，能快速定位是哪一环出错，而不是默默地给出错误结果。
- **关键流程要幂等/可重试。** 一次失败不应造成数据污染或重复推送。

### 2. 可延展性（Extensibility）
今天做的地基，要能支撑未来三年的演进，加新功能时不必推倒重来。

具体要求：
- **配置与逻辑分离。** 新闻源清单、关注的行业、筛选标准、推送目标等"会变的东西"，放在配置区/配置文件里，不写死在主逻辑中。改口味只改配置，不动核心代码。
- **模块化。** 抓取、筛选、推送各自独立，一块的实现变化不影响其他块。未来要接知识库（RAG）、换模型、加新输出渠道，都只动对应模块。
- **预留接口。** 对已知的未来方向（接 Obsidian 知识库做 RAG、增加研报/财报等新源、结果回流），结构上留好插入点，但不提前实现。

### 3. 多适配度（Adaptability）
系统要能适应不同的来源格式、运行环境和使用场景。

具体要求：
- **来源多样化。** 能处理不同格式的输入（RSS、网页、API、PDF 等），新增一类源不需要重构。
- **环境无关。** 代码能在本地（开发）和云端（GitHub Actions / 服务器）一致运行，不依赖某台机器的特殊设置。敏感信息（API Key）通过环境变量注入，绝不硬编码。
- **输出可切换。** 推送渠道（飞书、微信、邮件等）可配置、可扩展。

---

## 工作方式约定

- **小步验证。** 每个功能先在小范围跑通、看到真实结果，再往下做。绝不一次写一大坨然后祈祷它能跑。
- **先确认需求与逻辑，再写实现。** 复杂功能先把规格（spec）定清楚。
- **代码之外同样重要。** 提示词、配置、源策略、文档，与代码同等对待，一并维护。

---

## 检验清单（每次新增代码前自问）

- [ ] 这段代码依赖的外部源/服务挂了，系统会怎样？有降级方案吗？
- [ ] 未来想改的东西（源、行业、模型、渠道），是不是放在了配置里、而非写死？
- [ ] 出错时，我能从输出里看出是哪一环、为什么错吗？
- [ ] 这段代码换到云端能原样跑吗？有没有依赖本机的特殊设置？
- [ ] 敏感信息有没有硬编码进代码？

---

_本原则确立于项目初期，作为后续所有开发决策的基准。_
